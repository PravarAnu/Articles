// Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with their data type.

var string = "Hello";
var bool = true;
var undef = undefined;
var nulli = null;


console.log(`Value is ${string} and type is ${typeof(string)}`);
console.log(`Value is ${bool} and type is ${typeof(bool)}`);
console.log(`Value is ${undef} and type is ${typeof(undef)}`);
console.log(`Value is ${nulli} and type is ${typeof(nulli)}`);
